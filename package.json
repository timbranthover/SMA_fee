{
  "name": "ups-investment-screener",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node local-server.mjs",
    "check": "node --check app.js && node --check lib/shared-config.js && node --check lib/catalog.js && node --check api/search.js && node --check api/detail.js && node --check local-server.mjs",
    "test": "node --test tests/*.test.mjs",
    "build": "npm run check && npm test"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED"
}
